% use utf8;
<h3>Keyboard Shortcuts</h3>
<hr>
<ul>
% my @keys        = grep { $sidebar->{meta}{$_}{shortcut} } %{ $sidebar->{meta} };
% my @sorted_keys = sort { $sidebar->{meta}{$a}{shortcut} <=> $sidebar->{meta}{$b}{shortcut} } @keys;
% for my $k (@sorted_keys) {
%   my $v        = $sidebar->{meta}{$k};
%   my $shortcut = $v->{shortcut};
%   my $text     = $v->{text};
  <li> <kbd><%= $shortcut %></kbd> <%= $text %> </li>
% }
</ul>
