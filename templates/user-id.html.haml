- layout 'default';
- title $user->name . '님';

- my $status_id = 1;

%ul
  %li
    %i.icon-user
    %a{:href => "#{url_for('/guests/' . $user->id)}"} #{$user->name}
    %span (#{$user->user_info->birth})
  %li
    %i.icon-map-marker
    = $user->user_info->address
  %li
    %i.icon-envelope
    %a{:href => "mailto:#{$user->email}"}= $user->email
  %li= $user->user_info->phone
  %li
    %span #{$user->user_info->height} cm,
    %span #{$user->user_info->weight} kg

%div
  %p
    %a{:href => '/guests/#{$user->id}'}= $user->name
    님
    - if ( $user->update_date ) {
      %strong= $user->update_date->ymd
      %span 방문
    - }
    %div
      %span.label= $user->user_info->height
      %span.label= $user->user_info->weight
      %span.label.label-info.search-label
        %a{:href => "#{url_with('/search')->query([q => $user->user_info->bust])}///#{$status_id}"}= $user->user_info->bust
      %span.label.label-info.search-label
        %a{:href => "#{url_with('/search')->query([q => '/' . $user->user_info->waist . '//' . $status_id])}"}= $user->user_info->waist
      %span.label= $user->user_info->hip
      %span.label= $user->user_info->thigh
      %span.label.label-info.search-label
        %a{:href => "#{url_with('/search')->query([q => '//' . $user->user_info->arm])}/#{$status_id}"}= $user->user_info->arm
      %span.label= $user->user_info->leg
      %span.label= $user->user_info->knee

%h3 주문내역

%ul
  - for my $order ( $user->order_users ) {
  -   next unless $order->status;
  -
  -   my $rental = $order->rental_date ? $order->rental_date->ymd : q{};
  -   my $target = $order->target_date ? $order->target_date->ymd : q{};
  -   my $return = $order->return_date ? $order->return_date->ymd : q{};
  -
    %li
      %a{:href => "#{url_for('/orders/' . $order->id)}"}
        - if ($order->status->name eq '대여중') {
        -   if (calc_overdue($order->target_date, DateTime->now())) {
          %span.label.label-important 연체중
        -   }
        -   else {
          %span.label.label-important= $order->status->name
        -   }
          %span.highlight{:title => '대여일'}= $rental
          ~
          %span{:title => '반납예정일'}= $target
        - }
        - else {
          %span.label= $order->status->name
          %span.highlight{:title => '대여일'}= $rental
          ~
          %span.highlight{:title => '반납일'}= $return
        - }
  - }
